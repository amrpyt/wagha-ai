---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "react-dropzone, sharp, and pdfjs-dist are installed"
    - "next.config.ts includes pdfjs-dist in serverExternalPackages"
    - "/uploads/ directory exists and is gitignored"
  artifacts:
    - path: "src/lib/upload"
      provides: "Upload validation, storage, and processing utilities"
    - path: "src/lib/ai"
      provides: "Nano Banana 2 client and prompt management"
    - path: "src/components/upload"
      provides: "DropZone, ProjectForm, GenerationProgress components"
    - path: "uploads/"
      provides: "UUID-named uploaded and rendered files"
  key_links: []
---

<objective>
Install Phase 2 dependencies, update Next.js config for pdfjs-dist, and create directory structure for new libraries.
</objective>

<execution_context>
@D:/Vibe Coding/02-Real PRojects with Clints/Wagha-ai/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-upload-ai-integration/02-RESEARCH.md
@src/types/database.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install npm packages</name>
  <files>package.json</files>
  <action>
    Install the following packages as production dependencies:
    npm install react-dropzone sharp pdfjs-dist

    Verify all three installed with: npm list react-dropzone sharp pdfjs-dist
  </action>
  <verify>
    <automated>npm list react-dropzone sharp pdfjs-dist 2>&1 | grep -E "react-dropzone|sharp|pdfjs-dist"</automated>
  </verify>
  <done>Three packages listed in npm list output with version numbers</done>
</task>

<task type="auto">
  <name>Task 2: Update next.config.ts for pdfjs-dist</name>
  <files>next.config.ts</files>
  <action>
    Read the existing next.config.ts first.

    Add 'pdfjs-dist' to the serverExternalPackages array.
    The existing array is: ['@supabase/supabase-js', '@supabase/ssr', 'pdfmake-rtl']
    After update: ['@supabase/supabase-js', '@supabase/ssr', 'pdfmake-rtl', 'pdfjs-dist']

    Keep sharp OFF the list — it is already auto-opted-out by Next.js.
  </action>
  <verify>
    <automated>grep -n "pdfjs-dist" next.config.ts</automated>
  </verify>
  <done>next.config.ts contains 'pdfjs-dist' in serverExternalPackages array</done>
</task>

<task type="auto">
  <name>Task 3: Create directory structure and .gitignore</name>
  <files>uploads/.gitignore</files>
  <action>
    Create the following empty directories (with placeholder files so git tracks them):
    - src/lib/upload/ (create index.ts placeholder)
    - src/lib/ai/ (create index.ts placeholder)
    - src/components/upload/ (create index.ts placeholder)
    - uploads/ (create .gitignore with "*" to ignore all files inside)

    The uploads/.gitignore should contain:
    *
    !.gitignore

    This ensures the directory exists in the repo but all uploaded files are ignored.
  </action>
  <verify>
    <automated>ls -la src/lib/upload src/lib/ai src/components/upload uploads/ 2>&1</automated>
  </verify>
  <done>All four directories exist, uploads/.gitignore is present and correct</done>
</task>

</tasks>

<verification>
Wave 1 is complete when: npm list shows packages installed, next.config.ts has pdfjs-dist, and directory structure exists.
</verification>

<success_criteria>
All 3 packages installed, next.config.ts updated, 4 directories created with placeholder files.
</success_criteria>

<output>
After completion, create `.planning/phases/02-upload-ai-integration/02-01-SUMMARY.md`
</output>
